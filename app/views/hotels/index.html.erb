<h1>All hotels</h1>

<% @hotels.each do |hotel| %>
  <h4><%= link_to hotel.title, hotel %></h4>
  <p>
    <%= image_tag(hotel.photo_url(:thumb), class: "img-rounded pull-left", style: "margin-right: 20px") %>
    <%= hotel.description %><br>
    <b>Breakfast:</b> <%= hotel.breakfast ? 'Yes' : 'No' %><br>
    <b>Price:</b> $<%= hotel.price %><br>
    <b>Author:</b> <%= hotel.user.email %>
  </p>
  <hr>
<% end %>
